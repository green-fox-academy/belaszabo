# - Create a variable named `nimals`
#   with the following content: `["kuty", "macsk", "cic"]`
# - Add all elements an `"a"` at the end
nimals = ["kuty", "macsk", "cic"]
for i in range(0,len(nimals)):
    nimals[i] =  nimals[i] + "a"
print(nimals)